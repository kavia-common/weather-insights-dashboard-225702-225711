# Frontend environment variables

# Optional: override Open‑Meteo base URL (default used if unset)
REACT_APP_API_BASE=https://api.open-meteo.com

# Unused when Open‑Meteo is active (kept for optional OpenWeatherMap experiments)
REACT_APP_OPENWEATHER_API_KEY=unused
